# Base URL for API calls. During dev we use the Vite proxy.
VITE_API_BASE="/proxy"

# Path for the Create User endpoint (adjust to match Swagger)
# Common examples:
#   /api/users
#   /api/v1/users
#   /users/
VITE_CREATE_USER_PATH="/api/users"
